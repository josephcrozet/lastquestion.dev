{
  "name": "lastquestion-dev",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "start": "bun run build && bun run preview",
    "astro": "astro",
    "format": "prettier --check --log-level warn .",
    "format:fix": "prettier --write --log-level warn .",
    "lint": "eslint --max-warnings 0 .",
    "lint:fix": "eslint --fix .",
    "knip": "knip",
    "check": "astro check --noSync --silent",
    "biome": "biome check",
    "ci": "astro sync --silent && bun run --parallel format lint knip check build",
    "fix": "bun run --sequential format:fix lint:fix",
    "preinstall": "npx only-allow bun",
    "postinstall": "simple-git-hooks"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.3",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.2",
    "@tailwindcss/typography": "^0.5.19",
    "@tailwindcss/vite": "^4.2.4",
    "astro": "^6.1.8",
    "tailwindcss": "^4.2.4"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^8.2.0",
    "@astrojs/check": "^0.9.8",
    "@biomejs/biome": "^2.4.12",
    "eslint": "^10.2.1",
    "eslint-plugin-astro": "^1.7.0",
    "knip": "^6.6.0",
    "only-allow": "^1.2.2",
    "prettier": "^3.8.3",
    "prettier-plugin-astro": "^0.14.1",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "simple-git-hooks": "^2.13.1",
    "typescript": "^6.0.3"
  },
  "simple-git-hooks": {
    "pre-push": "bun run ci"
  }
}
